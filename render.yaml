services:
  - type: web
    name: meeting-center
    env: node
    region: sgp
    buildCommand: npm install && npm run build
    startCommand: npm run start
    plan: free
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false

